import Link from "next/link";
import { directions } from "./content";
import { ModernHero, ModernNav } from "./ModernDesign";

export default function ModernGallery() {
  const options = directions.filter(d=>Number(d.id)>=6);
  return <div className="new-gallery"><header className="new-gallery-nav"><Link href="/" className="new-gallery-brand">creaweb.</Link><span>ROUND TWO / NEW DIRECTIONS</span><Link href="/designs?collection=editorial">Previous options ↗</Link></header><main><section className="new-gallery-intro"><span className="new-gallery-pill">Five new ways to show up.</span><h1>Different looks.<br /><span>Same Crea Web.</span></h1><p>Beyond editorial. Explore five completely different visual directions, built around your existing services, words and work.</p><a href="#new-options" className="new-gallery-jump">Find your direction ↓</a><div className="new-gallery-shapes" aria-hidden="true"><span /><span /><span>✳</span></div></section><section id="new-options" className="new-gallery-grid" aria-label="New designs 06 to 10">{options.map(d=><article key={d.id} className={`new-gallery-card new-gallery-card-${d.id}`}><div className="new-gallery-card-label"><span>{d.id.padStart(2,"0")}</span><span>{d.traits}</span></div><div className="new-gallery-preview"><div className="new-gallery-miniature" inert aria-hidden="true"><div className={`nx nx-${d.theme}`}><div className="nx-shell"><ModernNav /><ModernHero theme={d.theme} preview /></div></div></div><Link href={`/designs/${d.id}`} className="new-gallery-preview-link" aria-label={`Open ${d.name}, option ${d.id}`}><span>Explore {d.name} ↗</span></Link></div><div className="new-gallery-card-body"><div><h2><Link href={`/designs/${d.id}`}>{d.name}</Link></h2><span className="new-gallery-palette" aria-label="Colour palette">{d.palette.map(color=><i key={color} style={{backgroundColor:color}} />)}</span></div><h3>{d.short}</h3><p>{d.description}</p><Link className="new-gallery-open" href={`/designs/${d.id}`}>View full design <span aria-hidden="true">↗</span></Link></div></article>)}</section><section className="new-gallery-end"><h2>Which direction<br />feels right?</h2><p>Open a preview, try it on your phone, and choose a number from 06–10. You can also combine elements from different directions.</p><Link href="/designs?collection=editorial">Revisit the first five options ↗</Link></section></main><footer className="new-gallery-footer"><span>Crea Web / Design explorations</span><span>Your content. More possibilities.</span></footer></div>;
}
